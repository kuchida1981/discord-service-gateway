## 1. 準備と環境設定

- [ ] 1.1 `src/core/config.py` に `N8N_HEALTH_URL` 設定を追加する
- [ ] 1.2 `.env.example` に `N8N_HEALTH_URL` のサンプル値を追加する

## 2. コマンド登録基盤の拡張

- [ ] 2.1 `src/cli/register_commands.py` を修正し、`/dsg` -> `n8n` -> `health` の階層構造を定義する
- [ ] 2.2 `register_commands.py` がネストされたコマンドを正しく登録できるか確認する（デバッグログ等）

## 3. サービス層の実装 (n8n integration)

- [ ] 3.1 `src/services/n8n.py` を新規作成し、ヘルスチェックを行う関数を実装する
- [ ] 3.2 `httpx` を使用し、2.5秒のタイムアウト設定で GET リクエストを送信するロジックを実装する
- [ ] 3.3 レスポンスの検証（ステータスコード、JSON構造）を行い、成功/失敗の結果を返すようにする

## 4. API ルーティングとディスパッチの実装

- [ ] 4.1 `src/api/routes.py` を修正し、`/dsg` インタラクション受信時のオプションパースロジックを実装する
- [ ] 4.2 解析したサブコマンドに基づいて、`src/services/n8n.py` の関数を呼び出すようにする
- [ ] 4.3 最終的なレスポンス（`n8n status: ok ✅` 等）を Discord に返す

## 5. テストと検証

- [ ] 5.1 `/dsg n8n health` が正しくパースされ、サービス関数が呼ばれることを確認するテストコードを追加する
- [ ] 5.2 n8n が正常な場合と異常な場合の両方のシナリオで、正しいメッセージが返ることを確認する
- [ ] 5.3 実際にコマンドを登録し、Discord 上で階層構造が正しく表示されることを目視確認する
